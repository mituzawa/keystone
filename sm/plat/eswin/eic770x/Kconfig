# SPDX-License-Identifier: BSD-2-Clause
# Copy of the vendor platform/eswin/eic770x/Kconfig (eswincomputing/opensbi,
# opensbi-1.3-EIC7X): OpenSBI 1.3 reads Kconfig + configs/defconfig from the
# platform source dir, which PLATFORM_DIR points at this tree.

config PLATFORM_ESWIN_EIC770X
	bool
	select FDT
	select IPI_MSWI
	select IRQCHIP_PLIC
	select SERIAL_UART8250
	select TIMER_MTIMER
	select FDT_PMU
	default y
